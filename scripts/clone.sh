#!/usr/bin/env bash

# This script clone a bunch of git repositories from a list of URLs.
# It is intended to be run after the setup.sh script, which installs all the dependencies.

# TODO: Update list of repositories to clone
